# Set the host where shared preview links should resolve.
# Example: creator app runs at https://app.example.com
# shared viewer site runs at https://preview.example.com
# Replace this with a real deployed preview domain — do not keep the example.com placeholder.
VITE_SHARE_HOST=https://preview.example.com
